<UserControl x:Class="RealmListManager.UI.Screens.FirstTimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:screens="clr-namespace:RealmListManager.UI.Screens"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance screens:FirstTimeViewModel}"
             d:DesignWidth="560" d:DesignHeight="600">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="1"
                   Foreground="Black"
                   FontSize="24"
                   HorizontalAlignment="Center"
                   Text="Add a location to get started"/>
    </Grid>
</UserControl>
